# 修改日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。日期采用 `YYYY-MM-DD` 格式。

## 3.6.0 - 2026-09-04

### 安全

- 在高速 C++/MuPDF 后端前新增零第三方 crate 依赖的 Rust 安全守卫；Python 只启动守卫，守卫会在每次运行前核对编译期绑定的后端 EXE 与 MuPDF DLL SHA-256，阻止任一原生组件被单独替换。
- 使用 Windows Job Object 将守卫与后端限制在两个进程内，为每个进程设置默认 1,536 MiB 内存上限，并在守卫异常退出、超时或取消时终止后端，避免残留进程继续处理不可信 PDF。
- Python、Rust、C++ 三层统一校验任务 ID、页码、坐标、DPI、JPEG 质量、文件名和输出；限制工作区路径、PDF/清单/协议行/批次数量/单图像素与输出大小，并拒绝路径穿越、符号链接输出和不完整 JPEG。
- Rust 守卫启用 CFG、溢出检查、abort panic 与锁定的离线构建；C++ 后端启用 `/GS`、`/sdl`、CFG、ASLR、高熵 ASLR 和 DEP/NX。

### 兼容与测试

- 保留 v3.5 的 C++ 并行渲染和全局率失真规划器，安全守卫仅位于进程与协议边界；原生 worker 不可用时仍自动回退到 Python/MuPDF 路径。
- 新增后端篡改、清单路径穿越、越界请求、工作区逃逸、绕过守卫直启 C++ 与有界协议读取回归测试。
- 同机 A/B 基准中，未加守卫的 v3.5 路径为 16.790 秒，最终加固路径为 16.879 秒，总差异 0.53%；输出体积、PSNR、边缘相似度和原生文字均保持一致。
- 当前共 33 项 Python 回归测试和 4 项 Rust 单元测试全部通过。

## 3.5.0 - 2026-09-03

### 新增

- 新增全局率失真预算规划器：先为每张位图和 Figure 建立质量—体积阶梯，再在统一目标字节预算内选择兼顾公平性与视觉面积的最清晰组合。
- C++ worker 协议升级到 v2，新增 `LADDER` 批次；同一 Figure 只做一次最高 DPI 母版光栅化，相同 DPI 的 JPEG 变体共享缩放结果。
- 压缩结果与基准报告新增规划器启用状态、母版光栅化次数和编码变体数；桌面界面会标明“全局预算规划”。

### 性能

- 固定 `Automatica.pdf → 3.12 MiB` 完整基准为 9.501 秒，相对 v3.3.1 的 169.964 秒提升 17.889 倍，相对 v3.4.0 的 33.074 秒提升 3.481 倍。
- 19 张 Figure 仅执行 19 次母版光栅化，在一个原生批次内生成 266 个候选编码；完整 PDF 装配从 v3.4.0 基准的 27 次降为 2 次。
- 对不可能单靠独立位图达到目标的文档，使用严格下界提前跳过无效装配；本次输出距 3,271,557 字节目标仅 809 字节。

### 兼容与测试

- 原生 worker 缺失、协议不匹配、规划条件不满足或规划执行失败时，仍自动回退到 v3.4 的安全搜索路径。
- 新增质量阶梯母版复用、不同 DPI 尺寸、全局预算约束、公平质量下限、端到端规划压缩与文字保留回归测试；当前共 26 项测试全部通过。

## 3.4.0 - 2026-09-03

### 新增

- 新增独立 C++17 高速 Figure 渲染 worker，直接链接与 PyMuPDF 匹配的 MuPDF 原生库，并通过版本化协议向 Python 主引擎返回进度与结果。
- 原生 worker 使用最多 8 个长生命周期渲染线程；每个线程在整次精确定容搜索中只打开一次 PDF，Figure 任务并行渲染和 JPEG 编码。
- 桌面应用自动检测并使用原生 worker；二进制缺失、协议不匹配或执行异常时自动回退到原有 Python/MuPDF 路径。
- 原生任务支持安全取消，主程序结束时会关闭 worker，不留下后台进程或半成品输出。
- 打包流程除内置 worker 的桌面程序外，还会生成可单独分发的 Windows x64 worker 压缩包。

### 性能

- 移除 MuPDF 像素图在渲染与预览中的“先编码 PNG、再由 Pillow 解码”中转，直接读取原生像素缓冲区；透明图使用预乘 Alpha 正确转换，避免暗边和黑底回归。
- 为精确定容搜索加入 256 MiB 上限的 LRU 渲染缓存；相同 DPI、JPEG 质量与 Figure 区域不再重复渲染。
- 缩略图生成改为最多 4 个固定后台进程；每个进程只打开一次 PDF，并连续处理自己的 Figure 分区。
- 真实 `Automatica.pdf` 压缩到 3.12 MiB：169.964 秒降至 89.800 秒（1.893 倍），最终文件仍仅比目标小 83 字节。
- 启用 C++ worker 后，同一基准进一步降至 33.074 秒，相对原始版本提升 5.139 倍；结果比目标小 156 字节，PSNR 39.862 dB，原生文字完全保留，未检测到黑背景回归。
- 精确定容的质量档搜索改用带边界保护的体积插值，在保持最终微调能力的同时减少不必要的候选渲染。
- 97.92 MiB / 48 页压力文件：扫描与全部缩略图 7.547 秒，最大界面停顿 32.47 ms，主进程峰值内存 113.16 MiB。

### 测试

- 新增固定文件哈希、性能、内存、精确定容、PSNR、边缘相似度、原生文字一致性与黑背景检测的一键基准套件。
- 新增直接像素解码、单次 PDF 打开、Figure 渲染缓存、C++ 协议、持久会话、文字剔除、安全取消和工作进程上限回归测试；当前共 22 项测试全部通过。
- 验证 97.92 MiB 文件可在加载中安全取消，且不会残留资产或输出文件。

### 文档

- 修复 README 的 Star 曲线只显示 GitHub API 限制提示、无法呈现数据的问题。
- 改为读取 GitHub 官方 Stargazers API，并生成仓库内的自包含 SVG 曲线；同时提供可重复运行的更新脚本，避免依赖第三方图表服务。

## 3.3.1 - 2026-09-02

### 修复

- 左侧设置区与底部任务操作区改为独立布局；设置卡片可单独平滑滚动，压缩、取消和打开输出文件夹按钮始终固定在可视区域内。
- 修复最小窗口及 Windows 高 DPI 缩放下，左侧底部按钮显示不完整、看起来被右侧预览区遮挡的问题。
- 收窄并弱化侧栏滚动条，避免滚动条紧贴左右分栏边界造成遮挡错觉。

## 3.3.0 - 2026-09-02

### 界面

- 整体升级为商业级桌面应用视觉：全新品牌图标、玻璃感顶栏、隐私状态胶囊、渐变主操作按钮和更统一的圆角卡片体系。
- 左侧工作区改为清晰的三步操作流程，并加入动态任务状态灯、结构化结果反馈和安全提示卡片。
- Figure 区域加入分段筛选、卡片悬停光影、渐进淡入与惯性平滑滚动；高清预览和大型 PDF 加载保持温和动画反馈。
- 重新设计空状态与拖放入口，优化 1120–1400 像素窗口下的间距、字号和信息层级。

### 性能

- 97.92 MiB、48 页压力文件：扫描约 6.62 秒，扫描加全部缩略图约 8.86 秒，最大事件循环停顿约 41.16 ms，主进程峰值内存约 113.11 MiB，界面持续响应。
- 新增视觉效果保持轻量，Figure 扫描、缩略图和压缩仍在隔离后台任务中执行。

### 文档

- GitHub 默认主页改为英文，并保留可从页面顶部切换的完整简体中文 README。
- 操作演示说明同步提供英文与简体中文版本。
- 项目定位调整为日常 PDF 提交大小限制下的最小损失压缩，并明确介绍按图片选择处理的工作流。
- 增加 PowerPoint 矢量图可能出现黑色背景的已知限制，以及取消勾选该 Figure、保留原始内容的规避方法。
- 使用定制的双语 SVG 信息图替换 README 中的基础 Mermaid 工作原理图，清晰展示已勾选内容的优化路径与未勾选内容的原样保留路径。
- README 截图、GIF 和高清操作视频更新为 v3.3.0 商业化界面。

## 3.2.0 - 2026-09-01

### 新增

- 大文件读取卡片显示实时用时、界面响应状态和“停止读取”按钮。
- 新增 `tools/stress_test.py`，可生成高熵位图与密集矢量混合 PDF，并测量扫描时间、缩略图时间、事件循环最大停顿和峰值内存。
- 高清 Figure 预览生成期间显示呼吸纸张动画，不再留下无反馈的空白区域。
- README 嵌入基于真实 97.92 MiB PDF 录制的完整操作演示，并提供高清 MP4 与演示 PDF 下载。
- 新增 `tools/record_demo.py`，只捕获 Qt 应用窗口，能够复现“读取、预览、放大、选择、定容、输出”的演示流程。
- PDF 资产扫描支持逐页安全取消，取消后不生成或修改任何文件。

### 性能

- Figure 扫描与缩略图渲染移入独立子进程，避免复杂页面长时间占用 GUI 进程的 Python GIL。
- 预览卡片按每批 4 张渐进铺开，不再一次性创建全部控件。
- 缩略图直接使用 MuPDF 像素并编码为轻量 JPEG，移除“大 PNG 编码后立即解码缩小”的重复工作。
- 97.92 MB、48 页、每页 90 条矢量路径的压力文件：48 个 Figure 全部识别，扫描约 6.89 秒，扫描加缩略图约 9.24 秒，最大事件循环停顿约 125.81 ms，主进程峰值内存约 189.55 MB。
- 真实 12.25 MB PDF：识别 33 个图形，总加载约 2.63 秒，最大停顿约 43.25 ms。

## 3.1.0 - 2026-09-01

### 新增

- PDF 读取期间显示真实逐页进度与当前分析阶段。
- 主预览区加入 60 FPS 呼吸光晕、旋转轨道和自然错速的小光点动画。
- 加入会淡入淡出的友好提示文案，以及分析完成后的柔和列表过渡。

### 改进

- PDF 扫描、缩略图生成与界面事件循环完全分离，加载大型论文时窗口保持响应。
- 加载失败后自动恢复文件选择、进度条和操作状态。
- 为资产扫描进度增加单调性回归测试。

## 3.0.0 - 2026-08-31

### 新增

- 将桌面界面从 Tkinter 迁移至 Qt 6，采用接近 macOS 的浅色圆角卡片设计。
- 主界面双列 Figure 缩略图、平滑滚动、渐显动画和拖放 PDF。
- 约 240 DPI 高清全景预览，支持滚轮缩放、拖动、适应窗口与 100% 查看。
- 高 DPI 屏幕适配、动画进度条、完成 Toast 和单文件 EXE 构建。

### 修复

- 修复只读源文件在“直接复制”路径下导致输出文件仍为只读的问题。

## 2.6.0 - 2026-08-31

- 将 Figure 缩略图直接放入主界面，减少空白区域和多窗口切换。
- 每张预览卡片显示类型、名称、原 PDF 占用空间与放大入口。
- 扫描和缩略图生成转入后台，修复复杂科研论文读取时界面卡死的问题。

## 2.5.0 - 2026-08-31

- 引入 400 级质量搜索、Figure DPI 微调、单 Figure 1 DPI 分配与独立位图分辨率微调。
- 解决目标体积在相邻质量档之间大幅跳跃的问题，可精确生成接近 3.12 MiB 的结果。
- 调整为接近 PowerPoint 导出的高分辨率、适度 JPEG 压缩策略，优先保护图内小字符和细线。

## 2.4.0 - 2026-08-31

- 显示每个独立位图的实际 PDF 流占用。
- 为包含矢量指令、Form XObject 和位图的完整 Figure 提供空间占用估算。
- 选择摘要显示已选图形的估算总量，帮助优先处理占用最大的内容。

## 2.3.0 - 2026-08-31

- 根据图注识别完整论文 Figure，将内部位图、矢量线条、箭头和标签视为一个项目。
- 用户可按 Figure 预览和勾选，不再需要按页码指定排除项。
- 未勾选的 Figure 与正文保持原始 PDF 内容。

## 2.2.0 - 2026-08-31

- Figure 替代图统一转换为白底 RGB JPEG，不包含 Alpha 与软蒙版。
- 修复 `color pixmap must not have an alpha channel` 错误。
- 修复透明矢量图在部分阅读器中显示黑色背景的问题。

## 2.1.0 - 2026-08-31

- 支持位图和用户选择的矢量图形压缩。
- 增加图形排除与预览选择机制。
- 保留正文文字搜索、复制、链接、书签、表单与批注结构。

## 2.0.0 - 2026-08-31

- 停止整页栅格化，只降低 PDF 内图像和选中图形的清晰度。
- 保持页面文字、公式和未选中内容的原始清晰度。

## 1.0.0 - 2026-08-31

- 首个带界面的 Windows PDF 定容压缩原型。
- 支持选择 PDF、输入目标 MB/KB 和选择输出位置。
